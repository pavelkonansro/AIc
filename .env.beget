# AIc API Server Environment Variables for Beget Hosting
# Ready to use - just fill in your MySQL credentials

# Database Configuration (MySQL on Beget)
DB_HOST=localhost
DB_USER=konans6z_aic
DB_PASSWORD=vbftyjkT1984$
DB_NAME=konans6z_aic

# Server Configuration
PORT=3000
NODE_ENV=production

# Your Beget Domain
DOMAIN=konans6z.beget.tech

# Security
JWT_SECRET=konans6z-super-secret-jwt-key-change-this-in-production-2024

# Optional: External Services
# SENDGRID_API_KEY=your_sendgrid_key_for_emails
# REDIS_URL=redis://localhost:6379